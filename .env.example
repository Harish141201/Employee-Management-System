# Used by docker-compose.yml. Copy this file to ".env" in the same
# directory and fill in real values before running `docker compose up`.
# Never commit the real ".env" file.

MYSQL_ROOT_PASSWORD=changeme_pick_a_real_password
JWT_SECRET=change-this-to-a-long-random-string-before-any-real-deployment
ADMIN_SEED_USERNAME=admin
ADMIN_SEED_PASSWORD=ChooseAStrongPassword123!
