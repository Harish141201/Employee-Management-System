spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/employee_management}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Harish09@}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

server.port=${SERVER_PORT:8080}

app.cors.allowed-origin=${CORS_ALLOWED_ORIGIN:http://localhost:5173}

app.jwt.secret=${JWT_SECRET:this-is-a-local-dev-only-secret-change-me-before-deploying-anywhere-real-make-it-long}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

app.seed.admin-username=${ADMIN_SEED_USERNAME:admin}
app.seed.admin-password=${ADMIN_SEED_PASSWORD:Qazqaz1@}

