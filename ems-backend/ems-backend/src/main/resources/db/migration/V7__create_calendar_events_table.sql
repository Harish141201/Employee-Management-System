CREATE TABLE IF NOT EXISTS calendar_events (id BIGINT NOT NULL AUTO_INCREMENT, title VARCHAR(120) NOT NULL, event_date DATE NOT NULL, type VARCHAR(20) NOT NULL, description VARCHAR(500) NULL, PRIMARY KEY (id));
